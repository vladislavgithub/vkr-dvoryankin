\section{Обеспечение надёжности и безопасности сервиса}
\thispagestyle{plain}

% TODO: ~14 страниц --- прямое применение теории надёжности (Волков Д.А.)
% к собственной архитектуре сервиса.

\subsection{Расчёт показателей надёжности архитектуры}
% Декомпозиция системы на узлы: api, postgres, redis, caddy.
% Для каждого узла --- интенсивность отказов $\lambda$ и время
% восстановления $T_{в}$ из эксплуатационных данных или паспортных
% характеристик облачной инфраструктуры.
% Расчёт вероятности безотказной работы (ВБР) $P(t) = e^{-\lambda t}$,
% средней наработки на отказ (MTBF) и коэффициента готовности $k_г$.
% Структурная схема надёжности (последовательное и параллельное
% соединение узлов).
% Системный показатель доступности.

\subsection{Резервирование и репликация}
% Репликация PostgreSQL master-replica (streaming replication).
% Стратегия failover: автоматическое переключение на реплику.
% Резервное копирование: схема, частота, recovery point objective (RPO),
% recovery time objective (RTO).
% Идемпотентность операций записи и стратегия повторов.

\subsection{Безопасность}
% TLS 1.3 на всех соединениях (Caddy с Let's Encrypt).
% JWT-токены с коротким временем жизни и refresh-токены.
% Хеширование паролей: bcrypt/argon2.
% Защита от типовых уязвимостей OWASP Top 10:
% SQL-инъекции (параметризованные запросы),
% XSS (экранирование вывода во Flutter),
% CSRF (SameSite cookies, токены),
% IDOR (авторизация по принадлежности ресурса).
% Rate limiting на уровне API.

\subsection{Нагрузочное тестирование}
% Инструмент: k6 (или Locust).
% Сценарии: 50, 100, 500, 1000 одновременных активных подключений
% к интерактивной сессии.
% Метрики: время ответа (p50, p95, p99), пропускная способность,
% доля ошибок.
% Узкие места и оптимизации.

\subsection{Мониторинг и алертинг}
% Prometheus экспортеры: api (custom), postgres\_exporter, redis\_exporter,
% node\_exporter.
% Grafana дашборды: бизнес-метрики (активные сессии, ответы в сек.),
% технические метрики (latency, ошибки, утилизация ресурсов).
% Правила алертов: срабатывание при превышении SLA.

\subsection{Соответствие SLA}
% Целевая доступность 99{,}9\%.
% Расчёт по фактическим показателям мониторинга за период эксперимента.
% Сравнение расчётной и фактической надёжности.

\textbf{Выводы по главе 4.}
% Полученные показатели надёжности, обоснование SLA,
% связь с теоретическими положениями курса <<Надёжность>>.

\newpage
